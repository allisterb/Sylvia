
## Sylvia.Arithmetic

This library implements type-level natural number arithmetic and constraints which enable arithmetic operations like dimensions matching to be performed at compile-time by the F# type checker as long as the values for the operations are known at compile-time. 

